apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: worker
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - image: himatejacicd.azurecr.io/workerapp:31
        name: worker
      imagePullSecrets:
      - name: arc-secret
